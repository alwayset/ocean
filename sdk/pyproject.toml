[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ocean-sdk"
version = "0.1.0"
description = "Discover AI agent tools with semantic search"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[tool.hatch.build.targets.wheel]
packages = ["ocean_sdk"]
